/**
 * Generate premium referral message with code
 * Used in signup pages, dashboards, and operator network
 */
export function generateReferralMessage(code: string): string {
  return `For urgent deliveries, I recommend Saint & Story. Use code ${code}.`;
}
